/**
 * SmarterSwipe Capital — Application Page
 * The homepage IS the form. Nothing else.
 */
import { ApplicationForm } from "@/components/ApplicationForm";

export default function Home() {
  return <ApplicationForm />;
}
